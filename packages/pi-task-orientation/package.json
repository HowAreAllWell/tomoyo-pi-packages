{
  "name": "pi-task-orientation",
  "version": "0.1.0",
  "description": "Every request begins with a two-step orientation: (1) the model must assess available skills and load matching ones (or justify \"none\"), (2) it must check every AGENTS.md rule, apply always-applicable ones, and report skipped conditional rules with reasons. Replaces pi-mandatory-skill-resolution.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "skills",
    "rules",
    "agent"
  ],
  "license": "MIT",
  "type": "module",
  "main": "extensions/task-orientation.ts",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
